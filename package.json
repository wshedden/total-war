{
  "name": "total-war-v0",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server/src/index.js",
    "start": "NODE_ENV=production node server/src/index.js",
    "build": "node scripts/build-web.js",
    "test": "node scripts/sim-test.js",
    "data:refresh": "node scripts/data-refresh.js --refresh",
    "data:build": "node scripts/data-refresh.js --build"
  },
  "dependencies": {
    "d3-geo": "^3.1.1",
    "express": "^4.19.2",
    "topojson-client": "^3.1.0",
    "topojson-server": "^3.0.1",
    "world-atlas": "^2.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}